export class QueryField {
    field;

    queryChar;

    value;

    constructor(field, queryChar, value) {
        this.field = field;
        this.queryChar = queryChar;
        this.value = value;
    }
}
